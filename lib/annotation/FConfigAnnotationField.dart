import 'package:meta/meta_meta.dart';
@Target({TargetKind.field,})
/// 定义 `FconfigAnnotationField` 类，用于配置注解字段。
class FconfigAnnotationField {
  /// 构造 `FconfigAnnotationField` 实例，使用 `const` 修饰以支持常量构造。
  const FconfigAnnotationField();
}